{
  "name": "jsclient",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite & esbuild serviceWorker/sw.ts --bundle --outfile=public/sw.js --watch",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "esbuild": "0.20.1",
    "typescript": "^5.3.3",
    "vite": "^5.0.8",
    "vite-plugin-run": "^0.5.1"
  }
}
